language: cpp
compiler: gcc
script: 
    - mkdir build 
    - cd build
    - cmake .. 
    - make
    - make test
    - sudo pip install sphinx
    - sudo pip install breathe
    - sudo apt-get install doxygen
    - make docs
